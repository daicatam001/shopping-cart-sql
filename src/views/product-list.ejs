<%- include('./partials/header',{title:title}) %>


    <h1 class="mt-3">
        <%= title %>
    </h1>

    <div class="row">
        <% products.forEach(function(product){ %>
            <div class="col-md-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= product.title %>
                        </h5>
                        <div class="card-text">
                            <%= product.description %>
                        </div>
                        <h5 class="card-text">
                            <%= product.price %>
                        </h5>
                        <form class="d-inline" action="products/delete" method="post">
                            <input type="hidden" name="productId" value="<%= product.id  %> " >
                            <button class="btn btn-danger">Xóa</button>
                        </form>
                        <form class="d-inline" action="/cart" method="post">
                            <input type="hidden" name="productId" value="<%= product.id  %> " >
                            <button class="btn btn-primary">Mua</button>
                        </form>
                    </div>
                </div>
            </div>
            <% }); %>
    </div>
    <%- include('./partials/footer') %>